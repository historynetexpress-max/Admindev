# Copy this file to .env and fill secrets. DO NOT commit .env with real keys.

PORT=3000

# OpenAI API key for ChatGPT / streaming
OPENAI_API_KEY=sk-...

# Optional: default OpenAI model name to use for chatgpt
OPENAI_MODEL=gpt-4o-mini

# Google Generative AI API key (or use service account based approach & authenticate server)
GOOGLE_API_KEY=AIza...

# Optional: Google model to use
GOOGLE_MODEL=models/text-bison-001

# Set this to a comma-separated list of allowed origins for CORS (or "*" for quick local dev)
ALLOWED_ORIGINS=http://localhost:3000